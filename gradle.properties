# Indicates that AndroidX artifacts should be used instead of legacy support library artifacts.
android.useAndroidX=true
# Indicates that the Android Gradle plugin should automatically migrate third-party libraries to use AndroidX.
android.enableJetifier=true
