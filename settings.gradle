
pluginManagement {
    repositories {
        google()               // Android Gradle Plugin を解決する
        mavenCentral()
        gradlePluginPortal()   // Gradle Community プラグイン用
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'OpenGLTest'
include ':app'
